#!/usr/bin/env bash
# Root-level stub gradle wrapper for CI environments that invoke ./gradlew from repository root.
echo "Root stub gradle wrapper: skipping native Android build (Expo managed workflow)."
exit 0
